program order
  
  num1, num2, temp is int;

  read(num1);
  read(num2);

  if num1 > num2 then
    temp is num1;
    num1 is num2;
    num2 is temp;
  end;
  
  write({Menor: });
  write(num1);
  write({Maior: });
  write(num2);
end.