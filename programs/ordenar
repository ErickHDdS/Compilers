program order
  num1, num2, temp is int;
begin

  read(num1);
  read(num2);

  if num1 > num2 then
    temp = num1;
    num1 = num2;
    num2 = temp;
  end
  
  write({Menor: });
  write(num1);
  write({Maior: });
  write(num2);
end.