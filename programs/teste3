program 
    begin
        a, aux is int;
        b is float
        
        b = 0;
        in (a);
        in(b);
        if (a>b) then //troca variaveis
            aux = b;
            b = a;
            a = aux
        write(a;
        write(b)
    end;